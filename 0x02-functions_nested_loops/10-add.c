#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @a: added to b
 * @b: added to a
 * Return: sum a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
