#include <stdio.h>
/**
 * main - start point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char d;

	char e;

	d = 'a';
	e = 'A';
	while
		(d <= 'z') {
			putchar(d);
			d--;
		}
	while
		(e <= 'Z') {
			putchar(e);
			e--;
		}
	putchar('\n');
	return (0);
}
