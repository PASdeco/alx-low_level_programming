#include <stdio.h>
/**
 * main - start point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	int e;

	c = 'a';
	e = 0;
	while
		(e < 10) {
			putchar(e + '0');
			e++;
		}
	while
		(c <= 'a') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
