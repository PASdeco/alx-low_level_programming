#include <stdio.h>
/**
 * main - Writing a program that prints a new line with printf function
 * Return: 0 (Success)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
