#include "main.h"
/**
 * _memcpy - a function that copies memory area
 * @dest: input
 * @src: input
 * @n: total bnumber of bytes
 * Return: Always 0
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	int a = 0;
	int b = b;

	for (; a < b; b++)
	{
		dest[a] = src[a];
		n--;
	}
	return (dest);
}
